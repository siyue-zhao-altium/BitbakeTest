header:
  version: 14

target:
  - blink

repos:
  bitbake:
    url: "https://git.openembedded.org/bitbake"
    branch: "2.0"
    layers:
      .: excluded

  meta-test:
    layers:
      meta-test:

local_conf_header:
  standard: |
    TMPDIR = "/home/ubuntu/tmp"

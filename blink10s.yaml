header:
  version: 14

target:
  - blink

repos:
  bitbake:
    url: "https://git.openembedded.org/bitbake"
    branch: "2.0"
    layers:
      .: excluded

  meta-test:
    layers:
      meta-test:

local_conf_header:
  standard: |
    HELLO_MESSAGE = "HELLO FROM LOCAL.CONF IN blink10s.yaml"
    TMPDIR = "/home/ubuntu/tmp"
